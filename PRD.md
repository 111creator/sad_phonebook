# PRD — 전화번호 관리 시스템

## 1. 문제 정의
개인 연락처(이름, 전화번호)를 안전하게 저장하고 관리할 수 있는 웹 애플리케이션이 필요하다. 기존 메모장/엑셀 방식은 보안에 취약하므로, 암호화 저장 및 클라우드 기반 관리 시스템을 구축한다.

## 2. 목표
- Supabase(Postgres) + Vercel(배포) + GitHub(버전관리)로 전화번호 관리 웹앱을 만든다
- 개인정보(이름/전화번호)는 **암호화(AES-GCM)**되어 DB에 저장된다
- 로그인 없이 누구나 사용 가능한 단일 사용자 환경으로 구성한다

## 3. 사용자 스토리
- 사용자는 이름과 전화번호를 입력하여 연락처를 추가할 수 있다
- 사용자는 저장된 연락처 목록을 조회할 수 있다
- 사용자는 이름 또는 전화번호로 연락처를 검색할 수 있다
- 사용자는 기존 연락처를 수정할 수 있다
- 사용자는 연락처를 삭제할 수 있다

## 4. 핵심 기능
| 기능 | 설명 |
|------|------|
| Create | 이름, 전화번호 추가 |
| Read | 전체 목록 조회 |
| Update | 이름, 전화번호 수정 |
| Delete | 연락처 삭제 |
| Search | 이름/전화번호 부분일치 검색 |

## 5. 데이터 모델
```sql
CREATE TABLE contacts (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name VARCHAR(255) NOT NULL,        -- AES-GCM 암호화된 값
  phone_number VARCHAR(50) NOT NULL, -- AES-GCM 암호화된 값
  created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc', now()) NOT NULL
);
```

## 6. 보안/암호화 설계
- **암호화 방식**: AES-GCM (Web Crypto API)
- **암호화 대상**: name, phone_number 필드
- **키 관리**: PBKDF2로 파생된 256bit 키 사용
- **IV**: 저장 시 랜덤 생성, ciphertext에 포함
- **DB 저장값**: Base64(IV + ciphertext) 형태로 저장
- **검색 방식**: 클라이언트에서 전체 복호화 후 필터링 (소규모 데이터 기준)
- **트레이드오프**: 부분일치 검색은 복호화 후 클라이언트에서 처리하므로 대규모 데이터에는 비효율적

## 7. 기술 스택
- **Frontend**: HTML + Vanilla JS (Web Crypto API)
- **Database**: Supabase (Postgres)
- **배포**: Vercel
- **버전관리**: GitHub

## 8. 배포 전략
- GitHub 레포지토리에 코드 push
- Vercel에서 GitHub 연동 후 자동 배포
- 환경변수: Supabase URL, anon key

## 9. 범위 및 제외항목
- **포함**: CRUD, 검색, 암호화, Vercel 배포
- **제외**: 로그인/인증, 다중 사용자, 모바일 앱
